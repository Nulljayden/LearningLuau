Luau for Roblox
https://www.youtube.com/watch?v=LWL5QUdSQiA
formatted page with headers - https://docs.google.com/document/d/1ymideLiMU61-vergsWndsUudMkhHQ_gUIq-vn65bDSk/edit?usp=sharing

# Everything after the hashtag is a note


Variables:


local ExampleVariable1 = 5 #a regular local variable
ExampleVariable2 = 10 #This is a global variable, Dont use this
local myString = "Hello World!" #quotations make this a string variable
local value = "Hello ".."World!" #This simply prints "Hello World" after combining the variables
local variable #This will have a nil value since theres no values assigned to it
local boolVariable = true #sets a variable to true
local value1, value2 = 10, 15
Logic


local addition = 5 + 2
local Subtraction = 5 - 2
local multiplication = 5 * 2
local division = 5 / 2
advanced Logic


local floorDiv = 5 // 2 #Divides then rounds down to the nearest whole number
local modulusDiv = 5 % 2 #Gives the remainder
local exponentiation = 5 ^ 2 #Raises the number to the nearest power of another number
local doubleNegation = - - 5 #Makes the number Positive
If Statements


if true then            #"true" can be replaced with anything like 1 >= 0, just needs a question here
    print("this is true")
end
elseif 2 > 1 then        #if the prev condition is false, it will go down another layer
    print("2 is greater then 1")
else                #if all else is false, it goes here
    print("Thats false")
end
Functions


local function functionName()
    print("your code here")
end


local functionName = function() #this does the same thing as the prev one
    print("your code here")
end


functionName() #call a function in youe script


local function functionName(val1, val2) #this opens up these variables to the rest of the script
    print(val1)  #equal to 20 from calling the statement below
    print(val2)  #equal to 40 from calling the statement below
    return #not completely sure here but i think it sends the above prints to the console
end
functionName(20,40) #this changes the variable in the function by whatevers in the parentheses
Tables
Local ourTable = {12, “Text”}
print(ourTable[2]) #this will grab the 2nd variable from the table
ourTable[3] = “NewItem!” #this will add another variable to the table with a index of 3 or overrite the prev index 3 variable
table.insert(ourTable, “AddedVariable”) #allows you to add an item if you don't know how many are inside the table
Local ourTable[2] = nil #this will take away the value from a index but it wont remove it from the table, it will just be an empty value
table.remove(ourTable, 2) #this wil remove the value from the table but it will shift all the values after it down by one
ourTable[#ourTable] = nil #this will remove the last indexed variable
ourTable[“propertyCatagory”] = “New Property” #this will give you a property for the Variable (think of like a currency catagory with the properties set to $56)
ourTable[“PropertyCatagory”] = nil #this is to remove the catagory
Loops
For i = 1, 10, 1 do  #the first 1 is where it starts, the 10 is hoe many times it will run, and the second 1 is how many it goes up by after every loop. If a step count is not typed, it will assume 1
        print(“Loop!”)
End
While Loops
#these check for a condition before starting
While Variable2 > Variable1 do #this will keep looping until Variable2 is not greater then Variable1 (the values wwont change unless they are changed from within the script)
        print(“This is Looping”)
End
Repeat Loops
#these check for a condition at the end
repeat
        print(“sup”)
Until false
Threads
Local co1 = coroutine.create(function()
print(“Hello World”)
coroutine.yield() #this will not run with the prev statement, it requres the statement to be ran again and it will start from here the second time as everything above is dead
print(“Hello World 2”)
end)
print(coroutine.status(co1)) #this checks the staus, suspended has not been ran yet, dead has been ran
coroutine.resume(co1) #this will run the coroutine above unless its dead, after being ran, it dies


task.spawn(function() #basically just a quick function i think
print(“Hello World!”)
end)


print(“a”)
task.defer(function() #this will run last, that means, you see in console: a, c, b (defer)
print(“b (defer)”)
end)
print(“c”)


task.delay(3, function() #the number is the number of seconds before running
        print(“Hello World”)
end)


If dead then
        print(“The player died”)
else
        task.cancel(deathMessage) #this will cancel the proceeding task if deemed unnecessary
deathMessage = task.delay(5, function()
print(“You have died”)
end)
Other helpful commands
If typeof(Variable) == “Table” then #this check if the variable is a table
end
Console


prnt(ExampleVariable) #Prints a variable value to console
--Print("This is a comment") #Does not print anything, just a editor comment
print(string.sub(value, 1, 5)) #this will just print out a portion of the string based of the Variable(value), min(1), and max(5)
